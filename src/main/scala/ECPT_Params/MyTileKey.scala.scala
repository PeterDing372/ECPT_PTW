package ECPT.Params

import chisel3._
import chisel3.util._
import chipsalliance.rocketchip.config._
import freechips.rocketchip.tile.TileParams

// import chisel3.util._

// case object TileKey extends Field[TileParams]

// trait MyTileKey (q: Parameters, tileParams: TileParams) {

//     q.alterMap(Map( TileKey -> tileParams))
        

// } 


// class MyTileKey extends Config((site, here, up) => {
//   case TileKey => MyTileParams
// })

    


